{
  "$comment": "Per-repo values for the sydevs `workflow` plugin and its skills. The skills hold the rules. docs/ holds the stories.",
  "packageManager": "pnpm",
  "leanGate": {
    "command": ".claude/skills/pr-prep/check.sh",
    "full": ".claude/skills/pr-prep/check.sh --full",
    "contents": "See docs/rules/testing-reqs.md."
  },
  "contractStep": {
    "description": "Payload migrations. Attempt first, then hand off if it hangs.",
    "command": "timeout 300 pnpm db:migrations:create <name> --skip-empty < /dev/null",
    "onExit124": "Hand off to the user. Do not retry.",
    "rationale": "src/migrations/AGENTS.md"
  },
  "securityReview": {
    "triggerPattern": "src/plugins/access/|src/collections/(Clients|Managers)/|src/collections/[^/]+/endpoints/|src/app/.*/api/|src/plugins/storage/|webhook|src/payload.config.ts",
    "rationale": "Paths that own auth, client and manager records, endpoints, storage, and the Payload config."
  },
  "worktreeSetup": [
    "CI=true pnpm install",
    "scripts/ensure-test-db.sh"
  ],
  "worktreeNotes": ".env is git-tracked, so it is already present. The dev server needs a distinct PORT (shared local Postgres via push: true).",
  "deployTarget": "Railway (+ per-PR preview)",
  "devServer": {
    "command": "pnpm devsafe",
    "basePort": 3000,
    "healthPath": "/api/managers/me",
    "isolateDatabase": true,
    "databaseUrlEnv": "DATABASE_URL",
    "databaseNameTemplate": "sahajcloud_dev_{slug}"
  },
  "generatedFiles": [
    {
      "pattern": "^src/payload-types\\.ts$",
      "reason": "Generated by Payload. Run `pnpm generate:types` to regenerate. Edit the collection, field, or global config to change the types."
    },
    {
      "pattern": "^src/app/\\(payload\\)/admin/importMap\\.js$",
      "reason": "Generated. Run `pnpm generate:importmap`. To change it, edit the admin component imports in collections or globals."
    },
    {
      "pattern": "^src/migrations/.+\\.json$",
      "reason": "Generated by `pnpm db:migrations:create`. Never edit by hand. Create a new migration instead."
    },
    {
      "pattern": "^src/migrations/.+\\.ts$",
      "reason": "Generated by `pnpm db:migrations:create`. Do not augment (see src/migrations/AGENTS.md). For a backfill or schema tweak, tell the user and let them create a new migration."
    }
  ],
  "prAllowlistGlobs": [
    "package.json",
    "pnpm-lock.yaml",
    "**/*.md",
    ".claude/**",
    "src/payload-types.ts"
  ]
}
